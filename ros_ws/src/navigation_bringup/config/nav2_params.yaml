bt_navigator:
  ros__parameters:
    global_frame: map
    robot_base_frame: base_link
    odom_topic: odom
    bt_loop_duration: 10
    default_server_timeout: 20
    wait_for_service_timeout: 1000
    action_server_result_timeout: 900.0
    navigators: ["navigate_to_pose", "navigate_through_poses"]
    navigate_to_pose:
      plugin: "nav2_bt_navigator::NavigateToPoseNavigator"
    navigate_through_poses:
      plugin: "nav2_bt_navigator::NavigateThroughPosesNavigator"
    # 'default_nav_through_poses_bt_xml' and 'default_nav_to_pose_bt_xml' are use defaults:
    # nav2_bt_navigator/navigate_to_pose_w_replanning_and_recovery.xml
    # nav2_bt_navigator/navigate_through_poses_w_replanning_and_recovery.xml
    # They can be set here or via a RewrittenYaml remap from a parent launch file to Nav2.

    # plugin_lib_names is used to add custom BT plugins to the executor (vector of strings).
    # Built-in plugins are added automatically
    # plugin_lib_names: []

    error_code_name_prefixes:
      - assisted_teleop
      - backup
      - compute_path
      - dock_robot
      - drive_on_heading
      - follow_path
      - nav_thru_poses
      - nav_to_pose
      - route
      - spin
      - undock_robot
      - wait

controller_server:
  ros__parameters:
    # One control cycle per simulator step. /clock only advances every dt * decimation
    # (0.005 * 16 = 0.08 s), so anything above 12.5 Hz just re-plans on identical odom
    # and scan data and floods the log with "Control loop missed its desired rate".
    controller_frequency: 12.5
    costmap_update_timeout: 0.30
    min_x_velocity_threshold: 0.001
    min_y_velocity_threshold: 0.01
    min_theta_velocity_threshold: 0.001
    failure_tolerance: 0.3
    progress_checker_plugins: ["progress_checker"]
    goal_checker_plugins: ["general_goal_checker"] # "precise_goal_checker"
    controller_plugins: ["FollowPath"]
    odom_topic: odom
    use_realtime_priority: false
    enable_stamped_cmd_vel: false

    # Progress checker parameters
    progress_checker:
      plugin: "nav2_controller::SimpleProgressChecker"
      required_movement_radius: 0.5
      movement_time_allowance: 10.0
    # Goal checker parameters
    #precise_goal_checker:
    #  plugin: "nav2_controller::SimpleGoalChecker"
    #  xy_goal_tolerance: 0.25
    #  yaw_goal_tolerance: 0.25
    #  stateful: True
    general_goal_checker:
      stateful: True
      plugin: "nav2_controller::SimpleGoalChecker"
      xy_goal_tolerance: 0.25
      yaw_goal_tolerance: 0.25
    FollowPath:
      plugin: "dwb_core::DWBLocalPlanner"
      debug_trajectory_details: True
      min_vel_x: -0.1
      min_vel_y: -0.1
      max_vel_x: 0.5
      max_vel_y: 0.25
      max_vel_theta: 1.
      min_speed_xy: -3.0
      max_speed_xy: 3.0
      min_speed_theta: -1.
      acc_lim_x: 3.0
      acc_lim_y: 3.0
      acc_lim_theta: 3.0
      decel_lim_x: -3.0
      decel_lim_y: -3.0
      decel_lim_theta: -3.0
      vx_samples: 30
      vy_samples: 10
      vtheta_samples: 30
      sim_time: 1.5
      linear_granularity: 0.05
      angular_granularity: 0.025
      xy_goal_tolerance: 0.5
      path_length_tolerance: 1.0
      trans_stopped_velocity: 0.25
      short_circuit_trajectory_evaluation: True
      limit_vel_cmd_in_traj: False
      stateful: True
      critics: ["RotateToGoal", "Oscillation", "BaseObstacle", "ObstacleFootprint", "GoalAlign", "PathAlign", "PathDist", "GoalDist"]
      BaseObstacle.scale: 0.02
      ObstacleFootprint.scale: 1.0
      PathAlign.scale: 32.0
      GoalAlign.scale: 24.0
      PathAlign.forward_point_distance: 0.1
      GoalAlign.forward_point_distance: 0.1
      PathDist.scale: 32.0
      GoalDist.scale: 24.0
      RotateToGoal.scale: 32.0
      RotateToGoal.slowing_factor: 5.0
      RotateToGoal.lookahead_time: -1.0

      ####### END DWA ###########

      # plugin: "nav2_mppi_controller::MPPIController"
      # time_steps: 60 #56
      # model_dt: 0.05 #0.05
      # batch_size: 1500 #2000
      # vx_std: 0.2
      # vy_std: 0.2
      # wz_std: 0.4
      # vx_max: 1.5
      # vx_min: -0.5 #-0.35
      # vy_max: 0.5
      # vy_min: -0.5
      # wz_max: 1.0
      # wz_min: -1.0
      # iteration_count: 1
      # prune_distance: 1.7
      # transform_tolerance: 0.1
      # temperature: 0.3
      # gamma: 0.015
      # motion_model: "DiffDrive"
      # visualize: true
      # TrajectoryVisualizer:
      #   trajectory_step: 5
      #   time_step: 3
      # TrajectoryValidator:
      #   plugin: "mppi::DefaultOptimalTrajectoryValidator"
      #   collision_lookahead_time: 2.0
      #   consider_footprint: true
      # AckermannConstraints:
      #   min_turning_r: 0.2
      # critics: ["TwirlingCritic", "ConstraintCritic", "ObstaclesCritic", "GoalCritic", "GoalAngleCritic", "PathAlignCritic", "PathFollowCritic", "PathAngleCritic", "PreferForwardCritic", "VelocityDeadbandCritic"] #swaped cost with obstacles
      # ConstraintCritic:
      #   enabled: true
      #   cost_power: 1
      #   cost_weight: 4.0
      # GoalCritic:
      #   enabled: true
      #   cost_power: 1
      #   cost_weight: 5.0
      #   threshold_to_consider: 1.4
      # GoalAngleCritic:
      #   enabled: true
      #   cost_power: 1
      #   cost_weight: 6.0 #3.0
      #   threshold_to_consider: 1.0 #0.5
      # PreferForwardCritic:
      #   enabled: false #true
      #   cost_power: 1
      #   cost_weight: 1.0 #5.0
      #   threshold_to_consider: 2.5
      # ObstaclesCritic:
      #   enabled: true
      #   cost_power: 1
      #   repulsion_weight: 1.2 #1.5
      #   critical_weight: 2.0 #20.0
      #   consider_footprint: true
      #   collision_cost: 10000.0
      #   collision_margin_distance: 0.1
      #   near_goal_distance: 0.5
      # PathAlignCritic:
      #   enabled: true
      #   cost_power: 1
      #   cost_weight: 14.0 #14.0
      #   max_path_occupancy_ratio: 0.05
      #   trajectory_point_step: 4
      #   threshold_to_consider: 0.8 #0.5
      #   offset_from_furthest: 20
      #   use_path_orientations: true
      # PathFollowCritic:
      #   enabled: true
      #   cost_power: 1
      #   cost_weight: 6.0 #5.0 7 seems to be max
      #   offset_from_furthest: 5
      #   threshold_to_consider: 1.4
      # PathAngleCritic:
      #   enabled: true
      #   cost_power: 1
      #   cost_weight: 2.0 #2.0
      #   offset_from_furthest: 4
      #   threshold_to_consider: 1.0
      #   max_angle_to_furthest: 0.2 #1.0
      #   forward_preference: true
      # VelocityDeadbandCritic: #only forward?
      #   enabled: false
      #   cost_power: 1
      #   cost_weight: 35.0
      #   deadband_velocities: [0.1, 0.1, 0.1]
      # TwirlingCritic: #slowly backs up regardless of value, but works with new path planner
      #   enabled: true
      #   twirling_cost_power: 0.1
      #   twirling_cost_weight: 0.1 #10


local_costmap:
  local_costmap:
    ros__parameters:
      # Match the controller rate: at 4.0 Hz the local planner reused the same obstacle
      # data for ~3 control cycles, and 0.25 s left no margin under the controller's
      # costmap_update_timeout of 0.30 s. publish_frequency stays low, it only feeds RViz.
      update_frequency: 12.5
      publish_frequency: 2.0
      global_frame: odom
      robot_base_frame: base_link
      rolling_window: true
      width: 4
      height: 4
      resolution: 0.05
      footprint: "[ [0.25, 0.18], [0.25, -0.18], [-0.25, -0.18], [-0.25, 0.18] ]"
      plugins: ["voxel_layer", "inflation_layer"]
      inflation_layer:
        plugin: "nav2_costmap_2d::InflationLayer"
        cost_scaling_factor: 5.0
        inflation_radius: 0.4
      voxel_layer:
        plugin: "nav2_costmap_2d::VoxelLayer"
        enabled: True
        publish_voxel_map: True
        origin_z: 0.0
        z_resolution: 0.05
        z_voxels: 16
        max_obstacle_height: 0.5
        mark_threshold: 0
        observation_sources: point_cloud
        point_cloud:
          # A relative topic will be appended to the parent of the local_costmap namespace.
          # For example:
          #   * User chosen namespace is `tb4`.
          #   * User chosen topic is `scan`.
          #   * Topic will be remapped to `/tb4/scan` without `local_costmap`.
          #   * Use global topic `/scan` if you do not wish the node namespace to apply
          topic: lidar
          max_obstacle_height: 0.5
          clearing: True
          marking: True
          data_type: "PointCloud2"
          raytrace_max_range: 3.0
          raytrace_min_range: 0.0
          obstacle_max_range: 2.5
          obstacle_min_range: 0.0
      static_layer:
        plugin: "nav2_costmap_2d::StaticLayer"
        map_subscribe_transient_local: True
      always_send_full_costmap: True

global_costmap:
  global_costmap:
    ros__parameters:
      update_frequency: 1.0
      publish_frequency: 1.0
      global_frame: map
      robot_base_frame: base_link
      resolution: 0.05
      footprint: "[ [0.25, 0.18], [0.25, -0.18], [-0.25, -0.18], [-0.25, 0.18] ]"
      track_unknown_space: true
      plugins: ["static_layer", "obstacle_layer", "inflation_layer"]
      obstacle_layer:
        plugin: "nav2_costmap_2d::ObstacleLayer"
        enabled: True
        observation_sources: point_cloud
        point_cloud:
          # A relative topic will be appended to the parent of the global_costmap namespace.
          # For example:
          #   * User chosen namespace is `tb4`.
          #   * User chosen topic is `scan`.
          #   * Topic will be remapped to `/tb4/scan` without `global_costmap`.
          #   * Use global topic `/scan` if you do not wish the node namespace to apply
          topic: lidar
          max_obstacle_height: 0.5
          clearing: True
          marking: True
          data_type: "PointCloud2"
          raytrace_max_range: 3.0
          raytrace_min_range: 0.0
          obstacle_max_range: 2.5
          obstacle_min_range: 0.0
      static_layer:
        plugin: "nav2_costmap_2d::StaticLayer"
        map_subscribe_transient_local: True
      inflation_layer:
        plugin: "nav2_costmap_2d::InflationLayer"
        cost_scaling_factor: 3.0
        inflation_radius: 0.4
      always_send_full_costmap: True

# The yaml_filename does not need to be specified since it going to be set by defaults in launch.
# If you'd rather set it in the yaml, remove the default "map" value in the tb3_simulation_launch.py
# file & provide full path to map below. If CLI map configuration or launch default is provided, that will be used.
# map_server:
#   ros__parameters:
#     yaml_filename: ""

planner_server:
  ros__parameters:
    expected_planner_frequency: 20.0
    planner_plugins: ["GridBased"]
    costmap_update_timeout: 1.0
    GridBased:
      plugin: "nav2_navfn_planner::NavfnPlanner"
      tolerance: 0.75
      use_astar: false
      allow_unknown: true
      # plugin: "nav2_smac_planner::SmacPlannerHybrid"
      # tolerance: 0.5
      # allow_unknown: true


smoother_server:
  ros__parameters:
    smoother_plugins: ["simple_smoother"]
    simple_smoother:
      plugin: "nav2_smoother::SimpleSmoother"
      tolerance: 1.0e-10
      max_its: 1000
      do_refinement: True

behavior_server:
  ros__parameters:
    local_costmap_topic: local_costmap/costmap_raw
    global_costmap_topic: global_costmap/costmap_raw
    local_footprint_topic: local_costmap/published_footprint
    global_footprint_topic: global_costmap/published_footprint
    cycle_frequency: 10.0
    behavior_plugins: ["spin", "backup", "drive_on_heading", "assisted_teleop", "wait"]
    spin:
      plugin: "nav2_behaviors::Spin"
    backup:
      plugin: "nav2_behaviors::BackUp"
    drive_on_heading:
      plugin: "nav2_behaviors::DriveOnHeading"
    wait:
      plugin: "nav2_behaviors::Wait"
    assisted_teleop:
      plugin: "nav2_behaviors::AssistedTeleop"
    local_frame: odom
    global_frame: map
    robot_base_frame: base_link
    transform_tolerance: 0.1
    simulate_ahead_time: 2.0
    max_rotational_vel: 1.0
    min_rotational_vel: 0.4
    rotational_acc_lim: 3.2
    enable_stamped_cmd_vel: false

waypoint_follower:
  ros__parameters:
    loop_rate: 12.5
    stop_on_failure: false
    action_server_result_timeout: 900.0
    waypoint_task_executor_plugin: "wait_at_waypoint"
    wait_at_waypoint:
      plugin: "nav2_waypoint_follower::WaitAtWaypoint"
      enabled: True
      waypoint_pause_duration: 200

velocity_smoother:
  ros__parameters:
    smoothing_frequency: 12.5
    scale_velocities: False
    feedback: "OPEN_LOOP"
    max_velocity: [1.0, 1.0, 1.0]
    min_velocity: [-1.0, -1.0, -1.0]
    max_accel: [2.0, 2.0, 2.0]
    max_decel: [-2.0, -2.0, -2.0]
    odom_topic: "odom"
    odom_duration: 0.1
    deadband_velocity: [0.1, 0.1, 0.1]
    velocity_timeout: 1.0
    enable_stamped_cmd_vel: false

collision_monitor:
  ros__parameters:
    base_frame_id: "base_footprint"
    odom_frame_id: "odom"
    cmd_vel_in_topic: "cmd_vel_smoothed"
    cmd_vel_out_topic: "cmd_vel_safe"
    state_topic: "collision_monitor_state"
    transform_tolerance: 0.5
    enable_stamped_cmd_vel: False
    source_timeout: 1.0
    base_shift_correction: True
    stop_pub_timeout: 2.0
    holonomic: true       # Set to true for holonomic robots
    observation_sources: ["pointcloud"]
    pointcloud:
      type: "pointcloud"  
      topic: "lidar"
      transport_type: "raw"  # raw or/ with compression (zlib, draco, zstd)
      min_height: 0.1
      max_height: 0.5
      min_range: 0.1
      enabled: True
    # polygons: ["VelocityPolygonSlow"]
    polygons: ["FootprintApproach"]
    FootprintApproach:
      type: "polygon"
      action_type: "approach"
      footprint_topic: "local_costmap/published_footprint"
      time_before_collision: 1.2
      simulation_time_step: 0.1
      min_points: 6
      visualize: True
      enabled: True


    VelocityPolygonSlow:
      type: "velocity_polygon"
      action_type: "slowdown"
      slowdown_ratio: 0.5
      holonomic: true
      visualize: True
      enabled: True
      velocity_polygons: [
        "forward", "forward_left", "left", "backward_left",
        "backward", "backward_right", "right", "forward_right",
        "stopped"
      ]

      forward:
        points: "[[0.6, 0.4], [0.6, -0.4], [-0.3, -0.4], [-0.3, 0.4]]"
        linear_min: 0.05
        linear_max: 1.0
        direction_start_angle: -0.785   # -0.25pi
        direction_end_angle: 0.785      # 0.25pi
        theta_min: -1.0
        theta_max: 1.0

      forward_left:
        points: "[[0.55, 0.55], [0.55, -0.3], [-0.3, -0.3], [-0.3, 0.55]]"
        linear_min: 0.05
        linear_max: 1.0
        direction_start_angle: 0.0      # 0.0pi
        direction_end_angle: 1.571      # 0.5pi
        theta_min: -1.0
        theta_max: 1.0

      left:
        points: "[[0.4, 0.6], [0.4, -0.4], [-0.4, -0.4], [-0.4, 0.6]]"
        linear_min: 0.05
        linear_max: 1.0
        direction_start_angle: 0.785     # 0.25pi
        direction_end_angle: 2.356       # 0.75pi
        theta_min: -1.0
        theta_max: 1.0

      backward_left:
        points: "[[0.3, 0.55], [0.3, -0.3], [-0.55, -0.3], [-0.55, 0.55]]"
        linear_min: 0.05
        linear_max: 1.0
        direction_start_angle: -3.1415   # -pi
        direction_end_angle: -1.571      # -0.5pi
        theta_min: -1.0
        theta_max: 1.0

      backward:
        points: "[[0.3, 0.4], [0.3, -0.4], [-0.6, -0.4], [-0.6, 0.4]]"
        linear_min: 0.05
        linear_max: 1.0
        direction_start_angle: 2.356     # 0.75pi
        direction_end_angle: -2.356      # -0.75pi
        theta_min: -1.0
        theta_max: 1.0

      backward_right:
        points: "[[0.3, 0.3], [0.3, -0.55], [-0.55, -0.55], [-0.55, 0.3]]"
        linear_min: 0.05
        linear_max: 1.0
        direction_start_angle: 1.571     # 0.5pi
        direction_end_angle: 3.1415      # pi
        theta_min: -1.0
        theta_max: 1.0

      right:
        points: "[[0.4, 0.4], [0.4, -0.6], [-0.4, -0.6], [-0.4, 0.4]]"
        linear_min: 0.05
        linear_max: 1.0
        direction_start_angle: -2.356    # -0.75pi
        direction_end_angle: -0.785      # -0.25pi
        theta_min: -1.0
        theta_max: 1.0

      forward_right:
        points: "[[0.55, 0.3], [0.55, -0.55], [-0.3, -0.55], [-0.3, 0.3]]"
        linear_min: 0.05
        linear_max: 1.0
        direction_start_angle: -1.571   # -0.5pi
        direction_end_angle: 0.0        # 0.0pi
        theta_min: -1.0
        theta_max: 1.0

      # Stopped
      stopped:
        points: "[[0.4, 0.4], [0.4, -0.4], [-0.4, -0.4], [-0.4, 0.4]]"
        linear_min: 0.0
        linear_max: 0.05
        direction_start_angle: -3.1415
        direction_end_angle: 3.1415
        theta_min: -1.0
        theta_max: 1.0

